{
  "name": "pwm-splash-with-server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.17.2",
    "express": "^4.15.4",
    "moment": "^2.18.1",
    "mysql": "^2.14.1",
    "nodemon": "^1.12.0"
  },
  "scripts": {
    "server": "NODE_ENV=development nodemon server/index.js",
    "heroku-postbuild": "cd client && yarn --production=false && yarn run build",
    "build": "npm build ."
  }
}
